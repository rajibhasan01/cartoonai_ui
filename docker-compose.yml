version: '3.5'

services:
  node_app:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "4173:4173"

  python_app:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5005:5005"
